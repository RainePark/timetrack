<UserControl x:Class="WPFUI.MVVM.View.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WPFUI.MVVM.View"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="720" Background="Transparent">
    <ScrollViewer VerticalScrollBarVisibility="Visible"
                  Style="{StaticResource ScrollBarTheme}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200"></ColumnDefinition>
                <ColumnDefinition Width="*"></ColumnDefinition>
            </Grid.ColumnDefinitions>
            
            <TextBlock Text="Settings"
                       Foreground="White"
                       FontSize="40"
                       HorizontalAlignment="Left"
                       Margin="20 10 0 15">
            </TextBlock>
            
            <StackPanel Grid.Row="1" Grid.Column="0">
                <RadioButton Content="Category 1"
                             Height="50"
                             Foreground="White"
                             FontSize="25"
                             Style="{StaticResource SidebarButtonTheme}"
                             IsChecked="True"
                             Command="{Binding DashboardViewCommand}">
                </RadioButton>
            </StackPanel>
            
            <StackPanel Grid.Row="1" Grid.Column="1"></StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>
