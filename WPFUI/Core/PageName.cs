namespace WPFUI.Core;

// List of page names to be called when the menu is changed
// I have implemented it like this as it makes it easier to make changes if need
public enum PageName
{
    Undefined = 0, Dashboard, Usage, Blocks, Settings
}